# Appointy Calendar Sync - Environment Variables
# NOTE: For Docker deployment, configure via the admin panel at http://YOUR_IP:3000/admin
# These environment variables are only needed for local development

# Server port
PORT=3000

# Data directory for persistent config (Docker uses /data volume)
DATA_DIR=./data
